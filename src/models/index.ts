/**
 * Shared cross-resource shapes (`PaginatedResponse<T>`, `Money`, `Address`) and the per-resource
 * `requests.ts` / `responses.ts` / `schemas.ts` pattern (RFC §12/§13).
 *
 * Not yet implemented — lands in Phase 3 (Models, Validation & Shared Types), which depends only
 * on this phase's `zod` dependency and folder structure.
 *
 * @packageDocumentation
 */
export {};
