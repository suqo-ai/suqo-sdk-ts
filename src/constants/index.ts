/**
 * `SDK_VERSION` (embedded at build time), default timeouts, and path templates.
 *
 * `SDK_VERSION` and the `User-Agent: suqo-sdk-ts/{version}` header land in Phase 1 - Part 3
 * (Observability & Build Metadata) — that ticket picks the tsup `define`/inject mechanism
 * (architecture-review.md review gap #5). Default timeout constants land alongside `HttpCore` in
 * Phase 1 - Part 2.
 *
 * @packageDocumentation
 */
export {};
