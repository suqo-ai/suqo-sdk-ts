/**
 * `SuqoClient` — the composition root that wires `SdkConfig` + auth + `HttpCore` + resources
 * together (RFC §3). No business logic lives in this class itself.
 *
 * Not yet implemented — lands in Phase 4 (Pilot Resource), once `HttpCore` (Phase 1 - Part 2) and
 * the auth layer (Phase 2) both exist for it to wire together.
 *
 * @packageDocumentation
 */
export {};
