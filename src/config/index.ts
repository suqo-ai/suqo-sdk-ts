/**
 * Key-prefix → base URL inference and `SdkConfig` (SDK-SPEC.md §2, §4).
 *
 * Not yet implemented — lands in Ticket 1 alongside the new `SuqoError` hierarchy and the
 * `SuqoClient` shell. The previous implementation here (a selectable `production`/`staging`/
 * `local` environment, validated via zod) belonged to a retired RFC-based plan and has been
 * removed — see `docs/implementation-plan.md` Ticket 0 item 4.
 *
 * @packageDocumentation
 */
export {};
