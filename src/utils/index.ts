/**
 * Shared, dependency-free helpers (query-string building, pagination helpers, the `Logger`
 * interface) that don't belong to any single layer.
 *
 * Not yet implemented — grows on demand starting with the `Logger` interface in Phase 1 - Part 3
 * (Observability & Build Metadata).
 *
 * @packageDocumentation
 */
export {};
