/**
 * `TokenProvider` / `ApiKeyProvider` interfaces, default implementations, and single-flight
 * refresh-on-401 (RFC §7).
 *
 * Not yet implemented — lands in Phase 2 (Auth Layer), wired into `HttpCore`'s auth middleware
 * slot once Phase 1 - Part 2 exists.
 *
 * @packageDocumentation
 */
export {};
