/**
 * The `SuqoError` hierarchy and `mapHttpError()` (SDK-SPEC.md §7).
 *
 * Not yet implemented — lands in Ticket 1. The previous implementation here (`SDKError`,
 * `PermissionError`, a separate `TimeoutError`, and a `mapHttpError()` with no dual-shape 400
 * handling) belonged to a retired RFC-based plan and has been removed — see
 * `docs/implementation-plan.md` Ticket 0 item 4.
 *
 * @packageDocumentation
 */
export {};
