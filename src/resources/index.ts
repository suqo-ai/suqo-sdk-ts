/**
 * One class per API group (`auth`, `profile`, `business`, `products`, `subscriptions`,
 * `subscribers`, `transactions`, `payouts`, `kyc`, `smsCredits`, `configs`) — the SDK's public
 * surface (RFC §3, §10).
 *
 * Not yet implemented — the pilot resource lands in Phase 4, the remaining ten in Phase 5.
 *
 * @packageDocumentation
 */
export {};
